# Media and render products — regenerable, and large. The composition HTML,
# the plan and the brief are the source of truth and ARE committed.

# Screen capture + voiceover. These are the recorded originals; keep them backed
# up outside the repo. Placeholders are regenerated by the commands in README.md.
composition/assets/capture/
composition/assets/vo/

# Renders and review artifacts
*.mp4
*.mov
*.jpg
composition/snapshots/
composition/frames/

# HyperFrames render scratch
work-*/
composition/node_modules/
